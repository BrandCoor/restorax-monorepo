{
  "name": "restorax-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "db:up": "docker-compose up -d",
    "db:down": "docker-compose down",
    "start:backend": "npm run start:dev --workspace=backend",
    "start:web": "npm run dev --workspace=admin-web"
  }
}